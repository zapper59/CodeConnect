apply plugin: "java"
apply plugin: "eclipse"
apply plugin: "maven"

group = "com.toastedbits.codeconnect"
version = "0.1.0-SNAPSHOT"

dependencies {
	compile project(":code-connect-common")
	compile gradleApi();

	testCompile "junit:junit:4.12"
}
